BOT_TOKEN=123456:ABC...your_token
# Если оставить пустым — настроите через /set_channel (перешлите пост из нужного канала)
TARGET_CHANNEL_ID=
# Разрешённые администраторы (telegram user id, через запятую). Пусто = разрешить всем (для тестов).
ADMIN_IDS=111111111,222222222
TZ=Europe/Berlin
# По умолчанию:
DEFAULT_PARSE_MODE=HTML
DEFAULT_SILENT=false
DEFAULT_DISABLE_WEB_PAGE_PREVIEW=true
RSS_INCLUDE_LINK=true
